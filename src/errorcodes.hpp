// -*- mode: c++; c-file-style:"stroustrup"; -*-

#if !defined(ERRORCODES_H)
#define ERRORCODES_H

#include <string>
#include "../config.h"

const std::string errorcode(int rc);

#endif // ERRORCODES_H
